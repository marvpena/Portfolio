 let fruits = ["apple", "egg", "fish", "mango"]
 
 function test(data) {
    // iterate through array and return each in div
    let render = 'beans';
    for ( let i = 0; i < data.length; i++ ) {
            render += data[i]
    };

    // return render;
 }


 console.log('result: ', test.render);